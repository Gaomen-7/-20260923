server:
  port: 8090
  servlet:
    context-path: /mall-sys

db:
  username: root
  password: ""
  host: 127.0.0.1
  port: 3306
  name: project20206
  driverName: com.mysql.jdbc.Driver
  params: useUnicode=true&characterEncoding=UTF-8
spring:
  datasource:
    username: ${db.username}
    password: ${db.password}
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/project20206?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
  redis:
    host: localhost
    port: 6379
    # timeout: 10000
    # password: 123456
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.gec.domain
  mapper-locations:
    - classpath*:mappers/**/*.xml
web:
  uploadDir: D:/haida/space/upload/
