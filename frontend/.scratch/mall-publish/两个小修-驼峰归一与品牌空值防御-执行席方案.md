# 执行席方案 — Review 两个小修（驼峰归一 + 品牌空值防御）

> 指挥席：Doubao ｜ 执行席：atomcode CLI
> 依据：《代码Review报告-20260919.md》判断项 1、4
> 范围：仅 2 个前端文件，无新依赖、无路由/后端改动，改完 `npm run build` 应通过

---

## 修复 1：SimulateUser.vue 字段名统一为 camelCase（Review 判断项 1）

文件：`D:\vue-space\mypro5\src\components\SimulateUser.vue`

**背景**：该组件归一化输出用了 snake_case `goods_name`，与全站 `goodsName` 驼峰规范不一致。组件内部全部跟着用，需整体改名。

### 1.1 DEMO_GOODS 兜底数据（约 L74~L83）

8 行对象的键 `goods_name:` 全部改为 `goodsName:`：

```js
// 前：
var DEMO_GOODS = [
  { id: 9001, goods_name: '旗舰智能手机 Pro Max', price: 5999 },
  ...
// 后：
var DEMO_GOODS = [
  { id: 9001, goodsName: '旗舰智能手机 Pro Max', price: 5999 },
  ...
]
```
（其余 7 行同样把 `goods_name:` 改成 `goodsName:`）

### 1.2 loadGoods 归一化 map（约 L147~L154）

```js
// 前：
this.goodsList = list.slice(0, 12).map(function (item) {
  return {
    id: item.id,
    goods_name: item.goodsName || item.goods_name || ('商品' + item.id),
    price: item.price,
    mainImage: item.mainImage || item.main_image || ''
  }
})
// 后：仅输出键名 goods_name → goodsName；读取侧兼容保持不变
this.goodsList = list.slice(0, 12).map(function (item) {
  return {
    id: item.id,
    goodsName: item.goodsName || item.goods_name || ('商品' + item.id),
    price: item.price,
    mainImage: item.mainImage || item.main_image || ''
  }
})
```

### 1.3 模板两处引用（约 L24~L25）

```html
<!-- 前 -->
<span v-else>{{ g.goods_name.charAt(0) }}</span>
...
<div class="goods-name" :title="g.goods_name">{{ g.goods_name }}</div>
<!-- 后 -->
<span v-else>{{ g.goodsName.charAt(0) }}</span>
...
<div class="goods-name" :title="g.goodsName">{{ g.goodsName }}</div>
```

### 1.4 methods 中 5 处日志拼接（g.goods_name → g.goodsName）

| 方法 | 行附近 | 片段 |
|---|---|---|
| handleView | ~L183 | `'浏览商品 ' + g.goodsName + ' (id=' + g.id + ')'` |
| handleFav | ~L192 | `'取消收藏 ' + g.goodsName` |
| handleFav | ~L195 | `'收藏商品 ' + g.goodsName` |
| handleAddCart | ~L202 | `'加入购物车 ' + g.goodsName + ' 数量=' ...` |
| handleBuy | ~L205 | `'提交订单 ' + g.goodsName + ' ￥' + g.price` |

> 验收标准：全文件 `Grep "goods_name"` 应 0 命中（map 读取侧 `item.goods_name` 这一处是"读旧格式兜底"，可保留；若要求彻底干净也可一并删掉，不影响功能）。

---

## 修复 2：Brand.vue 空值防御（Review 判断项 4）

文件：`D:\vue-space\mypro5\src\components\Brand.vue`

**背景**：当某品牌 `logoName`/`logoUrl` 都为空时，`imgUrl` 会拼成 `.../Brand/showImg/undefined`，图片列出破图。

### 2.1 getBrandList 回填（约 L143~L147）

```js
// 前：
this.brandList.forEach(
  br=>{
    br.logoName = br.logoName || br.logoUrl;
    br.imgUrl = `${BASE}/Brand/showImg/${br.logoName}`;
  }
);
// 后：
this.brandList.forEach(
  br=>{
    br.logoName = br.logoName || br.logoUrl;
    br.imgUrl = br.logoName ? `${BASE}/Brand/showImg/${br.logoName}` : '';
  }
);
```

### 2.2 表格列模板（约 L26~L30）

```html
<!-- 前 -->
<el-table-column label="品牌LOGO" width="120">
  <template slot-scope="scope">
    <el-image :src="scope.row.imgUrl" style="width:80px; height:40px;" fit="contain"></el-image>
  </template>
</el-table-column>
<!-- 后 -->
<el-table-column label="品牌LOGO" width="120">
  <template slot-scope="scope">
    <el-image v-if="scope.row.imgUrl" :src="scope.row.imgUrl" style="width:80px; height:40px;" fit="contain"></el-image>
    <span v-else style="color:#c0c4cc; font-size:12px;">无图</span>
  </template>
</el-table-column>
```

---

## 执行步骤与验收

1. 按上面 hunks 改两个文件，**不要**顺手改其他文件（BASE 收敛属判断项 3，本次不做）。
2. `cd D:\vue-space\mypro5 && npm run build` → 期望 Build complete。
3. `Grep "goods_name" src/components/SimulateUser.vue` → 仅剩 map 读取侧那 1 处（或 0 处，按你取舍）。
4. 浏览器点验：
   - 模拟用户页：商品名正常显示、点浏览/收藏/加购，右侧日志里商品名正常中文（不再是 undefined）；
   - 品牌管理：10 个品牌 LOGO 正常出图；即使某行无 LOGO 也显示"无图"文字而非破图。
5. 改完把 build 结果和 Grep 命中数回报指挥席。

> 硬约束：webpack3，不得出现 `?.` / `async/await`；不新增 npm 依赖。
