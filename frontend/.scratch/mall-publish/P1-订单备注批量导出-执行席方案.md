# P1 订单备注/批量/导出 — 执行席实施方案

> 工单：P1-订单管理增强（需求规格说明书 #10 验收项补全）
> 预计时间：1.5-2 小时
> 包含三项：①备注编辑 ②批量删除 ③数据导出

---

## 一、任务目标

补全订单管理模块的三个验收关键项：
1. **备注编辑**：订单详情弹窗中备注从只读改为可编辑，保存到数据库
2. **批量操作**：表格复选框已存在，增加批量删除按钮（仅已完成/已取消可删）
3. **数据导出**：按当前筛选条件导出订单列表为 CSV 文件下载

---

## 二、现状确认（指挥席已探查）

### 已有基础
- `OrderInfo` 实体类**已有** `remark` 字段（String）
- `OrderInfoBO` **已有** `remark` 字段
- 详情弹窗**已显示**备注（只读，`{{ detailData.remark || '无' }}`）
- 表格**已有** `type="selection"` 复选框列（第50行），但无批量操作按钮
- `OrderInfoMapper.xml` 的 `getOrderList` 查询条件完整（8个筛选维度），导出可复用
- `OrderInfoServiceImpl` 有 `now()` 工具方法和状态校验模式可复用

### 缺失项
- 后端无 updateRemark / batchDelete / export 接口
- 前端无对应 API 方法和 UI 交互

---

## 三、后端修改（6个文件）

### 3.1 IOrderInfoService.java — 加 3 个方法签名

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\service\IOrderInfoService.java`

在接口中添加（import 需加 `java.util.List` 和 `javax.servlet.http.HttpServletResponse`、`com.gec.domain.search.OrderInfoSearch`）：
```java
/* 修改备注 */
void updateRemark(Integer id, String remark);

/* 批量删除订单（仅已完成/已取消） */
void batchDelete(List<Integer> ids);

/* 导出订单CSV */
void exportOrders(OrderInfoSearch search, HttpServletResponse response);
```

### 3.2 OrderInfoServiceImpl.java — 实现 3 个方法

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\service\impl\OrderInfoServiceImpl.java`

**添加 import**：
```java
import javax.servlet.http.HttpServletResponse;
import java.io.OutputStream;
import java.net.URLEncoder;
import java.util.List;
```

**在 `deleteOrder` 方法之后添加以下 3 个方法**：

```java
@Override
@Transactional
public void updateRemark(Integer id, String remark) {
    OrderInfo order = this.getById(id);
    if (order == null) {
        throw new RuntimeException("订单不存在");
    }
    OrderInfo update = new OrderInfo();
    update.setId(id);
    update.setRemark(remark);
    update.setUpdateTime(now());
    boolean ret = this.updateById(update);
    if (!ret) {
        throw new RuntimeException("修改备注失败");
    }
}

@Override
@Transactional
public void batchDelete(List<Integer> ids) {
    if (ids == null || ids.isEmpty()) {
        throw new RuntimeException("请选择要删除的订单");
    }
    for (Integer id : ids) {
        OrderInfo order = this.getById(id);
        if (order == null) {
            throw new RuntimeException("订单ID=" + id + " 不存在");
        }
        if (order.getOrderStatus() != 3 && order.getOrderStatus() != 4) {
            throw new RuntimeException("订单【" + order.getOrderNo() + "】不是已完成或已取消状态，不能删除");
        }
        /* 删除明细 */
        QueryWrapper<OrderItem> QW = new QueryWrapper<>();
        QW.eq("order_id", id);
        orderItemMapper.delete(QW);
        /* 删除主表 */
        this.removeById(id);
    }
}

@Override
public void exportOrders(OrderInfoSearch search, HttpServletResponse response) {
    /* 复用分页查询的 SQL，不分页，查全部符合条件的数据 */
    List<OrderInfoBO> list = orderInfoMapper.getOrderList(null, search).getRecords();

    response.setContentType("text/csv;charset=UTF-8");
    try {
        String fileName = URLEncoder.encode("订单列表_" + now().replaceAll("[-: ]", "") + ".csv", "UTF-8");
        response.setHeader("Content-Disposition", "attachment;filename=" + fileName);
        OutputStream out = response.getOutputStream();
        /* BOM 头，防止 Excel 打开中文乱码 */
        out.write(new byte[]{(byte) 0xEF, (byte) 0xBB, (byte) 0xBF});
        StringBuilder sb = new StringBuilder();
        /* 表头 */
        sb.append("订单编号,会员昵称,会员手机号,收货人,收货电话,收货地址,商品总额,折扣金额,运费,实收款,订单状态,付款状态,备注,下单时间\n");
        /* 数据行 */
        for (OrderInfoBO o : list) {
            sb.append(escapeCsv(o.getOrderNo())).append(",");
            sb.append(escapeCsv(o.getUserName())).append(",");
            sb.append(escapeCsv(o.getUserPhone())).append(",");
            sb.append(escapeCsv(o.getReceiverName())).append(",");
            sb.append(escapeCsv(o.getReceiverPhone())).append(",");
            sb.append(escapeCsv(o.getReceiverAddress())).append(",");
            sb.append(o.getTotalAmount() != null ? o.getTotalAmount() : "").append(",");
            sb.append(o.getDiscountAmount() != null ? o.getDiscountAmount() : "").append(",");
            sb.append(o.getFreight() != null ? o.getFreight() : "").append(",");
            sb.append(o.getActualAmount() != null ? o.getActualAmount() : "").append(",");
            sb.append(o.getOrderStatusName()).append(",");
            sb.append(o.getPayStatusName()).append(",");
            sb.append(escapeCsv(o.getRemark())).append(",");
            sb.append(escapeCsv(o.getCreateTime())).append("\n");
        }
        out.write(sb.toString().getBytes("UTF-8"));
        out.flush();
        out.close();
    } catch (Exception e) {
        throw new RuntimeException("导出失败：" + e.getMessage());
    }
}

/* CSV 字段转义：含逗号/引号/换行时用双引号包裹，内部双引号转义为两个 */
private String escapeCsv(String val) {
    if (val == null) return "";
    if (val.contains(",") || val.contains("\"") || val.contains("\n")) {
        return "\"" + val.replace("\"", "\"\"") + "\"";
    }
    return val;
}
```

> 注意：`orderInfoMapper.getOrderList(null, search)` 传 null page 时 MyBatis-Plus 不会分页，返回全部记录。如果 Page 参数为 null 时报空指针，则改为 `new Page(1, Integer.MAX_VALUE)`。

### 3.3 OrderInfoMapper.java — 确认方法签名

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\dao\OrderInfoMapper.java`

确认 `getOrderList` 方法签名为：
```java
IPage<OrderInfoBO> getOrderList(Page page, @Param("param") OrderInfoSearch param);
```
如果已有则无需修改。导出功能直接调用此方法传 null page。

### 3.4 OrderController.java — 加 3 个端点

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\controller\OrderController.java`

**添加 import**：
```java
import java.util.List;
```

**在 `delete` 方法之后、`exceptionFallback` 之前添加**：

```java
/* 7. 修改备注 */
@PutMapping("/updateRemark")
public R updateRemark(@RequestBody Map<String, Object> body) {
    Integer id = Integer.valueOf(body.get("id").toString());
    String remark = body.get("remark") != null ? body.get("remark").toString() : "";
    orderInfoService.updateRemark(id, remark);
    return R.ok();
}

/* 8. 批量删除 */
@DeleteMapping("/batchDelete")
public R batchDelete(@RequestBody Map<String, Object> body) {
    @SuppressWarnings("unchecked")
    List<Integer> ids = (List<Integer>) body.get("ids");
    orderInfoService.batchDelete(ids);
    return R.ok();
}

/* 9. 导出订单CSV */
@PostMapping("/export")
public void export(@RequestBody OrderInfoSearch param, HttpServletResponse response) {
    orderInfoService.exportOrders(param, response);
}
```

> 注意：export 端点返回 void，直接写 response，不经过 R 封装。前端需用 responseType: 'blob' 接收。

---

## 四、前端修改（2个文件）

### 4.1 pms_order.js — 加 3 个 API

**文件**：`D:\vue-space\mypro5\src\api\pms_order.js`

在文件末尾添加：
```javascript
/* 7.修改备注 */
export function updateRemark(id, remark) {
    return service({
        url: "/Order/updateRemark",
        method: "PUT",
        data: { id: id, remark: remark }
    });
}

/* 8.批量删除 */
export function batchDelete(ids) {
    return service({
        url: "/Order/batchDelete",
        method: "DELETE",
        data: { ids: ids }
    });
}

/* 9.导出订单CSV */
export function exportOrders(params) {
    return service({
        url: "/Order/export",
        method: "POST",
        data: params,
        responseType: 'blob'
    });
}
```

### 4.2 OrderManage.vue — 三项功能 UI 改造

**文件**：`D:\vue-space\mypro5\src\components\OrderManage.vue`

#### 改造 A：import 加新 API

将：
```javascript
import {
  list,
  detail,
  ship,
  updatePrice,
  cancel,
  deleteOrder
} from '@/api/pms_order.js'
```
改为：
```javascript
import {
  list,
  detail,
  ship,
  updatePrice,
  cancel,
  deleteOrder,
  updateRemark,
  batchDelete,
  exportOrders
} from '@/api/pms_order.js'
```

#### 改造 B：筛选区加导出按钮

在筛选区的 `el-form-item`（搜索/重置按钮那组）中，在重置按钮后加导出按钮：
```html
<el-button type="success" @click="doExport" icon="el-icon-download">导出</el-button>
```

#### 改造 C：表格上方加批量操作栏

在 `<!-- 状态 Tab -->` 之后、`<!-- 表格 -->` 之前，添加：
```html
<!-- 批量操作栏 -->
<div style="margin-bottom:8px;">
  <el-button type="danger" size="small" :disabled="selectedIds.length==0" @click="doBatchDelete" icon="el-icon-delete">
    批量删除({{ selectedIds.length }})
  </el-button>
  <span style="margin-left:10px; color:#999; font-size:12px;">仅已完成/已取消订单可删除</span>
</div>
```

#### 改造 D：表格加 selection 变化事件

给 `el-table` 添加 `@selection-change="handleSelectionChange"`：
```html
<el-table :data="tableData" style="width:100%" border @selection-change="handleSelectionChange">
```

#### 改造 E：详情弹窗备注改为可编辑

在详情弹窗中，将备注那一行：
```html
<el-descriptions-item label="备注" :span="2">{{ detailData.remark || '无' }}</el-descriptions-item>
```
改为：
```html
<el-descriptions-item label="备注" :span="2">
  <div v-if="!remarkEditing">
    <span>{{ detailData.remark || '无' }}</span>
    <el-button type="text" size="mini" @click="startEditRemark" style="margin-left:8px;">编辑</el-button>
  </div>
  <div v-else>
    <el-input v-model="editRemarkText" type="textarea" :rows="2" placeholder="请输入备注" style="margin-bottom:6px;"></el-input>
    <el-button type="primary" size="mini" @click="saveRemark">保存</el-button>
    <el-button size="mini" @click="remarkEditing=false">取消</el-button>
  </div>
</el-descriptions-item>
```

#### 改造 F：data 加新字段

在 data() 中添加：
```javascript
/* 批量选中 */
selectedIds: [],
/* 备注编辑 */
remarkEditing: false,
editRemarkText: ''
```

#### 改造 G：methods 加新方法

在 methods 中添加（放在 `doUpdatePrice` 之后）：

```javascript
/* 表格选中变化 */
handleSelectionChange(selection) {
  this.selectedIds = selection.map(item => item.id)
},

/* 批量删除 */
doBatchDelete() {
  if (this.selectedIds.length == 0) {
    this.$message.warning('请先选择要删除的订单')
    return
  }
  this.$confirm('确认删除选中的 ' + this.selectedIds.length + ' 条订单？仅已完成/已取消状态可删除，删除后不可恢复。', '警告', {
    type: 'error'
  }).then(() => {
    batchDelete(this.selectedIds).then(() => {
      this.$message.success('批量删除成功')
      this.selectedIds = []
      this.loadList()
    })
  }).catch(() => {})
},

/* 导出 */
doExport() {
  if (this.dateRange && this.dateRange.length == 2) {
    this.searchForm.startTime = this.dateRange[0]
    this.searchForm.endTime = this.dateRange[1]
  }
  var params = {
    orderNo: this.searchForm.orderNo || null,
    goodsName: this.searchForm.goodsName || null,
    keyword: this.searchForm.keyword || null,
    userName: this.searchForm.userName || null,
    receiverName: this.searchForm.receiverName || null,
    receiverPhone: this.searchForm.receiverPhone || null,
    orderStatus: this.searchForm.orderStatus,
    startTime: this.searchForm.startTime || null,
    endTime: this.searchForm.endTime || null
  }
  this.$message.info('正在导出，请稍候...')
  exportOrders(params).then(blob => {
    /* blob 下载 */
    var url = window.URL.createObjectURL(new Blob([blob]))
    var link = document.createElement('a')
    link.href = url
    link.setAttribute('download', '订单列表.csv')
    document.body.appendChild(link)
    link.click()
    document.body.removeChild(link)
    window.URL.revokeObjectURL(url)
    this.$message.success('导出成功')
  }).catch(() => {
    this.$message.error('导出失败')
  })
},

/* 开始编辑备注 */
startEditRemark() {
  this.editRemarkText = this.detailData.remark || ''
  this.remarkEditing = true
},

/* 保存备注 */
saveRemark() {
  updateRemark(this.detailData.id, this.editRemarkText).then(() => {
    this.$message.success('备注修改成功')
    this.detailData.remark = this.editRemarkText
    this.remarkEditing = false
  })
}
```

#### 改造 H：打开详情时重置备注编辑状态

在 `openDetail` 方法中，设置 `detailData` 后加一行：
```javascript
this.remarkEditing = false
```

---

## 五、验收标准

### 后端
1. `mvn compile` 编译通过
2. 重启 MallAPP
3. `PUT /Order/updateRemark`，body `{"id":1,"remark":"测试备注"}` → success，数据库 remark 字段更新
4. `DELETE /Order/batchDelete`，body `{"ids":[1,2]}` → 若状态合法则 success，若含非已完成/已取消则返回错误信息
5. `POST /Order/export`，body 筛选条件 → 返回 CSV 文件流，Content-Type 为 text/csv

### 前端
1. `npm run build` 编译通过
2. 订单详情弹窗中，备注旁有"编辑"按钮，点击后变 textarea，保存后更新显示
3. 表格勾选行后，批量删除按钮显示数量并可点击；未选中时禁用
4. 批量删除含非已完成/已取消订单时，后端返回错误，前端提示
5. 筛选区"导出"按钮点击后，下载 CSV 文件，用 Excel 打开中文无乱码
6. 导出的数据与当前筛选条件一致（含状态Tab筛选）

---

## 六、注意事项

1. **导出用 POST + blob**：因为筛选条件较多（8个参数），用 POST 传 body，前端 `responseType: 'blob'` 接收。不能用 GET（URL 太长）。
2. **CSV 中文乱码**：必须写 BOM 头（`0xEF 0xBB 0xBF`），否则 Excel 打开中文乱码。方案中已包含。
3. **批量删除状态校验**：逐条校验，遇到不合法的立即抛异常中断（@Transactional 回滚），错误信息含订单号便于定位。
4. **getOrderList(null, search)**：传 null page 不分页。如果 MyBatis-Plus 版本对 null page 处理有问题，改用 `new Page(1, Integer.MAX_VALUE)`。
5. **导出文件名**：后端用 `URLEncoder.encode` 处理中文文件名，前端下载时固定用 `订单列表.csv` 即可（后端文件名在 blob 模式下可能取不到）。
6. **备注编辑不限制状态**：任何状态的订单都可以改备注（商家内部备注），无需状态校验。
7. **webpack3 约束**：前端代码禁用可选链 `?.`，方案中已全部用 `||` 代替。
8. **el-table selection-change**：返回的是行对象数组，需 map 取 id。
9. **后端重启**：Java 代码修改后必须在 IDEA 重启 MallAPP。

---

## 七、修改文件清单

| # | 文件 | 操作 | 说明 |
|---|---|---|---|
| 1 | `IOrderInfoService.java` | 修改 | 加 updateRemark/batchDelete/exportOrders 签名 |
| 2 | `OrderInfoServiceImpl.java` | 修改 | 实现3方法 + escapeCsv 工具方法 |
| 3 | `OrderInfoMapper.java` | 确认 | getOrderList 签名（已有则不改） |
| 4 | `OrderController.java` | 修改 | 加 updateRemark/batchDelete/export 3端点 |
| 5 | `pms_order.js` | 修改 | 加3个API方法 |
| 6 | `OrderManage.vue` | 修改 | 备注编辑+批量删除+导出 UI 和逻辑 |
