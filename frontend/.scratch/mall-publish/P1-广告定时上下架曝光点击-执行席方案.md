# P1 广告管理增强 — 执行席实施方案

> 工单：P1-广告定时上下架+曝光点击统计（需求规格说明书 #14 验收项补全）
> 预计时间：1-1.5 小时
> 包含两项：①定时上下架生效 ②曝光/点击统计累加

---

## 一、任务目标

补全广告管理模块的两个验收关键项：
1. **定时上下架生效**：广告的投放起止时间真正生效，过期自动下线，未到开始时间显示"未开始"，列表显示四种状态（投放中/已下线/未开始/已过期）
2. **曝光/点击统计累加**：新增点击量字段，提供曝光+1和点击+1接口，前端可模拟触发并实时显示数据

---

## 二、现状确认（指挥席已探查）

### 数据库
- `tbl_advert` 表有 `start_time/end_time` 字段（String 类型，格式 yyyy-MM-dd），**但 status 切换不检查时间**
- 有 `current_views`（已展现量）和 `total_views`（售卖量），**无 click_count 字段**
- 5条测试数据

### 后端
- `AdvertController` 有 8 端点（list/add/update/delete/batchDelete/toggleStatus/upload/showImg）
- `Advert` 实体类无 clickCount 字段
- `pageAdvert` 查询只按 status 过滤，不计算时间有效性
- `toggleStatus` 直接改 status，不校验时间范围

### 前端
- `AdvertManage.vue` 有完整 CRUD，展现量用进度条显示
- 状态列只有 el-switch（投放中/已下线），无时间有效性判断
- 无点击量显示，无统计累加入口

---

## 三、数据库（1个字段）

```sql
USE project20206;
ALTER TABLE tbl_advert ADD COLUMN click_count INT DEFAULT 0 COMMENT '点击量' AFTER current_views;
```

---

## 四、后端修改（4个文件）

### 4.1 Advert.java — 加 clickCount 字段

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\domain\entity\Advert.java`

在 `private Integer currentViews;` 之后添加：
```java
private Integer clickCount;     // 点击量
```

### 4.2 IAdvertService.java — 加方法签名

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\service\IAdvertService.java`

在接口中添加：
```java
/* 刷新过期广告状态：将已过期且仍在投放中的广告批量下线 */
int refreshExpired();

/* 曝光量+1 */
void incrementView(Integer id);

/* 点击量+1 */
void incrementClick(Integer id);
```

### 4.3 AdvertServiceImpl.java — 实现新方法 + 改造 pageAdvert 计算有效状态

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\service\impl\AdvertServiceImpl.java`

**添加 import**：
```java
import java.time.LocalDate;
```

**改造 pageAdvert 方法**：在 `return baseMapper.selectPage(page, qw);` 之前，对查询结果逐条计算有效状态。将原方法替换为：

```java
@Override
public IPage<Advert> pageAdvert(Page page, AdvertSearch search) {
    QueryWrapper<Advert> qw = new QueryWrapper<>();
    if (search.getAdvertName() != null && !search.getAdvertName().trim().isEmpty()) {
        qw.like("advert_name", search.getAdvertName().trim());
    }
    if (search.getPosition() != null && !search.getPosition().isEmpty()) {
        qw.eq("position", search.getPosition());
    }
    if (search.getStatus() != null) {
        qw.eq("status", search.getStatus());
    }
    qw.orderByDesc("id");
    IPage<Advert> result = baseMapper.selectPage(page, qw);
    /* 逐条计算有效状态，写入 transient 字段 */
    String today = LocalDate.now().toString();
    for (Advert a : result.getRecords()) {
        a.setEffectiveStatus(calcEffectiveStatus(a, today));
    }
    return result;
}

/* 计算广告有效状态：0=已下线 1=投放中 2=未开始 3=已过期 */
private Integer calcEffectiveStatus(Advert a, String today) {
    if (a.getStatus() == null || a.getStatus() == 0) return 0;
    String start = a.getStartTime();
    String end = a.getEndTime();
    if (start != null && !start.isEmpty() && today.compareTo(start) < 0) return 2;
    if (end != null && !end.isEmpty() && today.compareTo(end) > 0) return 3;
    return 1;
}
```

**在 Advert 实体类中加 transient 字段**（注意：需要在 Advert.java 中加）：
```java
@com.baomidou.mybatisplus.annotation.TableField(exist = false)
private Integer effectiveStatus;  // 有效状态：0=已下线 1=投放中 2=未开始 3=已过期（非DB字段，查询时计算）
```

**添加新方法实现**（放在 `toggleStatus` 之后）：

```java
@Override
@Transactional
public int refreshExpired() {
    String today = LocalDate.now().toString();
    QueryWrapper<Advert> qw = new QueryWrapper<>();
    qw.eq("status", 1);
    qw.isNotNull("end_time");
    qw.lt("end_time", today);
    List<Advert> expiredList = this.list(qw);
    int count = 0;
    for (Advert a : expiredList) {
        Advert update = new Advert();
        update.setId(a.getId());
        update.setStatus(0);
        update.setUpdateTime(now());
        this.updateById(update);
        count++;
    }
    return count;
}

@Override
@Transactional
public void incrementView(Integer id) {
    Advert advert = this.getById(id);
    if (advert == null) throw new RuntimeException("广告不存在");
    int current = advert.getCurrentViews() != null ? advert.getCurrentViews() : 0;
    Advert update = new Advert();
    update.setId(id);
    update.setCurrentViews(current + 1);
    boolean ret = this.updateById(update);
    if (!ret) throw new RuntimeException("曝光量更新失败");
}

@Override
@Transactional
public void incrementClick(Integer id) {
    Advert advert = this.getById(id);
    if (advert == null) throw new RuntimeException("广告不存在");
    int current = advert.getClickCount() != null ? advert.getClickCount() : 0;
    Advert update = new Advert();
    update.setId(id);
    update.setClickCount(current + 1);
    boolean ret = this.updateById(update);
    if (!ret) throw new RuntimeException("点击量更新失败");
}
```

### 4.4 AdvertController.java — 加 3 个端点

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\controller\AdvertController.java`

在 `toggleStatus` 方法之后、`upload` 方法之前添加：

```java
/* 7. 刷新过期广告状态（将已过期且仍在投放中的广告批量下线） */
@PostMapping("/refreshExpired")
public R refreshExpired() {
    int count = advertService.refreshExpired();
    return R.ok().put("count", count);
}

/* 8. 曝光量+1 */
@PostMapping("/incrementView/{id}")
public R incrementView(@PathVariable("id") Integer id) {
    advertService.incrementView(id);
    return R.ok();
}

/* 9. 点击量+1 */
@PostMapping("/incrementClick/{id}")
public R incrementClick(@PathVariable("id") Integer id) {
    advertService.incrementClick(id);
    return R.ok();
}
```

> 注意：原端点编号 7/8 是 upload/showImg，新增的端点放在 toggleStatus 之后即可，编号不影响功能。

---

## 五、前端修改（2个文件）

### 5.1 pms_advert.js — 加 3 个 API

**文件**：`D:\vue-space\mypro5\src\api\pms_advert.js`

在文件末尾添加：
```javascript
/* 7.刷新过期广告状态 */
export function refreshExpired() {
    return service({
        url: "/Advert/refreshExpired",
        method: "POST"
    });
}

/* 8.曝光量+1 */
export function incrementView(id) {
    return service({
        url: `/Advert/incrementView/${id}`,
        method: "POST"
    });
}

/* 9.点击量+1 */
export function incrementClick(id) {
    return service({
        url: `/Advert/incrementClick/${id}`,
        method: "POST"
    });
}
```

### 5.2 AdvertManage.vue — 四项 UI 改造

**文件**：`D:\vue-space\mypro5\src\components\AdvertManage.vue`

#### 改造 A：import 加新 API

将：
```javascript
import {
  list,
  addAdvert,
  updateAdvert,
  deleteAdvert,
  batchDelete,
  toggleStatus
} from '@/api/pms_advert.js'
```
改为：
```javascript
import {
  list,
  addAdvert,
  updateAdvert,
  deleteAdvert,
  batchDelete,
  toggleStatus,
  refreshExpired,
  incrementView,
  incrementClick
} from '@/api/pms_advert.js'
```

#### 改造 B：操作按钮区加"刷新过期状态"

在操作按钮区，批量删除按钮后添加：
```html
<el-button type="warning" size="small" @click="doRefreshExpired" icon="el-icon-refresh">刷新过期状态</el-button>
```

#### 改造 C：展现量列改为"曝光/点击"，加点击量

将原"展现量"列：
```html
<el-table-column label="展现量" width="160">
  <template slot-scope="scope">
    <div style="display:flex; align-items:center;">
      <el-progress :percentage="getViewPercent(scope.row)" :stroke-width="8" style="flex:1;"></el-progress>
      <span style="margin-left:6px; font-size:12px; color:#909399;">
        {{ scope.row.currentViews }}/{{ scope.row.totalViews || '不限' }}
      </span>
    </div>
  </template>
</el-table-column>
```
改为：
```html
<el-table-column label="曝光/点击" width="180">
  <template slot-scope="scope">
    <div style="display:flex; align-items:center; margin-bottom:4px;">
      <el-progress :percentage="getViewPercent(scope.row)" :stroke-width="6" style="flex:1;"></el-progress>
    </div>
    <div style="font-size:12px; color:#909399;">
      曝光:{{ scope.row.currentViews || 0 }} | 点击:{{ scope.row.clickCount || 0 }}
      <span v-if="scope.row.currentViews > 0" style="color:#67C23A;">
        ({{ getClickRate(scope.row) }}%)
      </span>
    </div>
  </template>
</el-table-column>
```

#### 改造 D：状态列改为四种状态标签 + switch

将原状态列：
```html
<el-table-column label="状态" width="100" align="center">
  <template slot-scope="scope">
    <el-switch
      v-model="scope.row.status"
      :active-value="1"
      :inactive-value="0"
      active-text="投放中"
      inactive-text="已下线"
      @change="val=>handleToggleStatus(scope.row, val)">
    </el-switch>
  </template>
</el-table-column>
```
改为：
```html
<el-table-column label="状态" width="130" align="center">
  <template slot-scope="scope">
    <el-tag :type="getEffectiveTagType(scope.row.effectiveStatus)" size="mini" style="margin-bottom:4px;">
      {{ getEffectiveLabel(scope.row.effectiveStatus) }}
    </el-tag>
    <div>
      <el-switch
        v-model="scope.row.status"
        :active-value="1"
        :inactive-value="0"
        active-text="投放"
        inactive-text="下线"
        @change="val=>handleToggleStatus(scope.row, val)">
      </el-switch>
    </div>
  </template>
</el-table-column>
```

#### 改造 E：操作列加"模拟曝光/点击"按钮

将操作列：
```html
<el-table-column label="操作" width="120" fixed="right">
  <template slot-scope="scope">
    <el-button type="text" size="mini" @click="openEdit(scope.row)">编辑</el-button>
    <el-button type="text" size="mini" style="color:#f56c6c;" @click="handleDelete(scope.row)">删除</el-button>
  </template>
</el-table-column>
```
改为：
```html
<el-table-column label="操作" width="200" fixed="right">
  <template slot-scope="scope">
    <el-button type="text" size="mini" @click="openEdit(scope.row)">编辑</el-button>
    <el-button type="text" size="mini" @click="doIncrementView(scope.row)">曝光</el-button>
    <el-button type="text" size="mini" style="color:#409EFF;" @click="doIncrementClick(scope.row)">点击</el-button>
    <el-button type="text" size="mini" style="color:#f56c6c;" @click="handleDelete(scope.row)">删除</el-button>
  </template>
</el-table-column>
```

#### 改造 F：methods 加新方法

在 methods 中添加（放在 `handleSelectionChange` 之后）：

```javascript
/* 有效状态标签 */
getEffectiveLabel(status) {
  if (status == 0) return '已下线'
  if (status == 1) return '投放中'
  if (status == 2) return '未开始'
  if (status == 3) return '已过期'
  return '未知'
},
getEffectiveTagType(status) {
  if (status == 0) return 'info'
  if (status == 1) return 'success'
  if (status == 2) return 'warning'
  if (status == 3) return 'danger'
  return ''
},

/* 点击率 */
getClickRate(row) {
  if (!row.currentViews || row.currentViews == 0) return 0
  return ((row.clickCount || 0) / row.currentViews * 100).toFixed(1)
},

/* 刷新过期状态 */
doRefreshExpired() {
  this.$confirm('将所有已过期且仍在投放中的广告批量下线，确认执行？', '提示', {
    type: 'warning'
  }).then(() => {
    refreshExpired().then(resp => {
      this.$message.success('已刷新，下线 ' + (resp.count || 0) + ' 条过期广告')
      this.loadList()
    })
  }).catch(() => {})
},

/* 模拟曝光 */
doIncrementView(row) {
  incrementView(row.id).then(() => {
    row.currentViews = (row.currentViews || 0) + 1
  })
},

/* 模拟点击 */
doIncrementClick(row) {
  incrementClick(row.id).then(() => {
    row.clickCount = (row.clickCount || 0) + 1
  })
},
```

#### 改造 G：loadList 中 Number() 转换加 clickCount

在 `loadList` 的 `data.forEach` 中添加：
```javascript
item.clickCount = Number(item.clickCount || 0)
```

---

## 六、验收标准

### 数据库
1. `tbl_advert` 新增 `click_count` 字段，默认值 0

### 后端
2. `mvn compile` 编译通过，重启 MallAPP
3. 列表接口返回数据含 `effectiveStatus` 字段（0/1/2/3），`clickCount` 字段
4. `POST /Advert/refreshExpired` → 返回下线的过期广告数量
5. `POST /Advert/incrementView/{id}` → success，该广告 currentViews +1
6. `POST /Advert/incrementClick/{id}` → success，该广告 clickCount +1

### 前端
7. `npm run build` 编译通过
8. 列表状态列显示四种标签（投放中/已下线/未开始/已过期），下方仍有 switch 可手动切换
9. 曝光/点击列显示进度条 + 曝光数 + 点击数 + 点击率
10. 操作列有"曝光"和"点击"按钮，点击后对应数字+1（无需刷新页面）
11. "刷新过期状态"按钮点击后，过期广告自动下线，提示下线数量

---

## 七、注意事项

1. **effectiveStatus 是 transient 字段**：不在数据库中，查询时根据当前时间和 start_time/end_time 计算。Advert.java 中需加 `@TableField(exist = false)` 注解。
2. **时间比较用字符串**：start_time/end_time 是 String 类型（yyyy-MM-dd），直接用 `today.compareTo(start)` 比较即可，无需转 Date。
3. **refreshExpired 只下线不过期的**：只处理 status=1 且 end_time < 今天的广告，不影响已下线和未开始的。
4. **曝光/点击接口是增量更新**：先查当前值再 +1，用 updateById 只更新对应字段，避免覆盖其他字段。
5. **前端模拟按钮**：因为系统没有前端广告位页面，用操作列的"曝光"/"点击"按钮模拟统计累加，实际项目中应由广告位展示页面调用。
6. **switch 与 effectiveStatus 共存**：switch 控制数据库 status 字段（手动上下线），effectiveStatus 标签显示时间有效性。两者可能不一致（如手动上线但时间未到，显示"未开始"），这是正常的。
7. **webpack3 约束**：前端禁用可选链 `?.`，方案中已全部用 `||` 代替。
8. **后端重启**：Java 代码修改后必须在 IDEA 重启 MallAPP。

---

## 八、修改文件清单

| # | 文件 | 操作 | 说明 |
|---|---|---|---|
| 1 | SQL | 执行 | tbl_advert 加 click_count 字段 |
| 2 | `Advert.java` | 修改 | 加 clickCount + effectiveStatus(transient) 字段 |
| 3 | `IAdvertService.java` | 修改 | 加 refreshExpired/incrementView/incrementClick 签名 |
| 4 | `AdvertServiceImpl.java` | 修改 | pageAdvert 计算有效状态 + 实现3方法 |
| 5 | `AdvertController.java` | 修改 | 加3端点 |
| 6 | `pms_advert.js` | 修改 | 加3个API |
| 7 | `AdvertManage.vue` | 修改 | 四状态标签+点击量列+刷新过期+模拟曝光/点击按钮 |
