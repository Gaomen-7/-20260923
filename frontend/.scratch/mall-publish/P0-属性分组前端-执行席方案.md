# P0 属性分组前端 — 执行席实施方案

> 工单：P0-属性分组前端（需求规格说明书 #5，版块5）
> 预计时间：45-60 分钟
> 说明：交接文档原标注"后端已就绪"，经探查 AttrGroupController 为空壳（仅注释无端点），需同步补全后端 4 端点 + Service delete 方法

---

## 一、任务目标

实现属性分组管理页面，达到需求规格说明书验收标准：
1. 左侧类别树，点击分类后右侧显示该分类下的属性分组列表
2. 支持分组的新增、编辑、删除
3. 分组字段：分组名称、所属分类、描述、图标、排序
4. 导航栏 P=6「属性分组」点击后不再空白

---

## 二、现状确认（指挥席已探查）

### 数据库
- 表 `tbl_goods_attr_group` 已存在，字段：id, group_name, category_id, descript, icons, sort, create_date, update_date
- 现有7条测试数据（均 category_id=11）：主体/基本信息/存储/屏幕/电池信息/操作系统/网络支持

### 后端
- `GoodsAttrGroup.java` 实体类完整（id/groupName/categoryId/descript/icons/sort，继承 BaseEntity）
- `IAttrGroupService` 有 listAttrGroup/addAttrGroup/updateAttrGroup，**缺 delete**
- `AttrGroupServiceImpl` 实现了 list/add/update，**缺 delete**
- `AttrGroupController` **完全空壳**，只有注释占位，无任何端点
- `GoodsAttrController.groupOptions/{categoryId}` 已存在（规格参数页面的分组下拉用），无需重复实现

### 前端
- 无 `AttrGroupManage.vue` 组件
- 无 `pms_attrGroup.js` API 文件
- `router/index.js` 无 `/attrGroup` 路由
- `App.vue` 导航栏已有 P=6 → `#/attrGroup` 入口（无需修改）
- 参考页面 `GoodsAttrManage.vue` 结构完整（左侧类别树+右侧表格+对话框），可直接套用布局

---

## 三、后端修改（3个文件）

### 3.1 IAttrGroupService.java — 加 delete 方法签名

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\service\IAttrGroupService.java`

在接口中添加：
```java
void deleteAttrGroup(Integer id);
```

### 3.2 AttrGroupServiceImpl.java — 实现 delete

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\service\impl\AttrGroupServiceImpl.java`

在 `updateAttrGroup` 方法之后添加：
```java
/*删除属性分组*/
@Override
public void deleteAttrGroup(Integer id){
    int cnt = attrGroupMapper.deleteById(id);
    if(cnt!=1){
        throw new RuntimeException("删除属性分组失败");
    }
}
```

### 3.3 AttrGroupController.java — 补全 4 个端点

**文件**：`E:\space\Project\test\mall-sys\src\main\java\com\gec\controller\AttrGroupController.java`

将整个类替换为以下内容（保留 package/import/class 声明，替换方法体）：

```java
package com.gec.controller;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.gec.components.FileTemplate;
import com.gec.domain.entity.GoodsAttrGroup;
import com.gec.service.IAttrGroupService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/AttrGroup")
public class AttrGroupController extends BaseController {
    @Autowired
    private IAttrGroupService attrGroupService;

    /* 1.分页查询属性分组列表 */
    @PostMapping(
            value="/list/{page}/{limit}/{categoryId}",
            produces="application/json;charset=UTF-8"
    )
    public R list(
            @PathVariable("page") Integer page,
            @PathVariable("limit") Integer limit,
            @PathVariable("categoryId") Integer categoryId){
        try{
            Page frmPage = newPage(page, limit);
            return R.convertPage(attrGroupService.listAttrGroup(frmPage, categoryId));
        }catch(Exception e){
            e.printStackTrace();
            return R.err(e);
        }
    }

    /* 2.新增属性分组 */
    @PostMapping(
            value="/add",
            produces="application/json;charset=UTF-8"
    )
    public R add(@RequestBody GoodsAttrGroup attrGroup){
        try{
            attrGroupService.addAttrGroup(attrGroup);
            return R.ok();
        }catch(Exception e){
            e.printStackTrace();
            return R.err(e);
        }
    }

    /* 3.修改属性分组 */
    @PutMapping(
            value="/update",
            produces="application/json;charset=UTF-8"
    )
    public R update(@RequestBody GoodsAttrGroup attrGroup){
        try{
            attrGroupService.updateAttrGroup(attrGroup);
            return R.ok();
        }catch(Exception e){
            e.printStackTrace();
            return R.err(e);
        }
    }

    /* 4.删除属性分组 */
    @DeleteMapping(
            value="/delete/{id}",
            produces="application/json;charset=UTF-8"
    )
    public R delete(@PathVariable("id") Integer id){
        try{
            attrGroupService.deleteAttrGroup(id);
            return R.ok();
        }catch(Exception e){
            e.printStackTrace();
            return R.err(e);
        }
    }

    @ExceptionHandler(Exception.class)
    public R exceptionFallback(Exception E){
        E.printStackTrace();
        return R.err(E);
    }

    @Override
    protected FileTemplate getFileTemplate() {
        return null;
    }
}
```

> 注意：删除未用的 import（OptionMapper、QueryWrapper、List、OptionVO），如果 IDE 报未使用警告不影响编译，但建议清理。

---

## 四、前端修改（3个文件）

### 4.1 新建 pms_attrGroup.js

**文件**：`D:\vue-space\mypro5\src\api\pms_attrGroup.js`

```javascript
import service from '@/network/request.js'

/* 1.分页查询属性分组列表 */
export function list(page, limit, categoryId) {
    return service({
        url: `/AttrGroup/list/${page}/${limit}/${categoryId}`,
        method: "POST"
    });
}

/* 2.新增属性分组 */
export function addAttrGroup(data) {
    return service({
        url: "/AttrGroup/add",
        method: "POST",
        data: data
    });
}

/* 3.修改属性分组 */
export function updateAttrGroup(data) {
    return service({
        url: "/AttrGroup/update",
        method: "PUT",
        data: data
    });
}

/* 4.删除属性分组 */
export function deleteAttrGroup(id) {
    return service({
        url: `/AttrGroup/delete/${id}`,
        method: "DELETE"
    });
}
```

### 4.2 新建 AttrGroupManage.vue

**文件**：`D:\vue-space\mypro5\src\components\AttrGroupManage.vue`

```vue
<template>
  <div>
    <el-row :gutter="16">
      <!-- 左侧：类别树 -->
      <el-col :span="6">
        <el-card class="box-card">
          <div slot="header" class="clearfix">
            <span>请选择分类</span>
          </div>
          <el-tree
            :data="categoryList"
            :props="defaultProps"
            accordion
            node-key="id"
            highlight-current
            @node-click="handleNodeClick">
          </el-tree>
        </el-card>
      </el-col>

      <!-- 右侧：分组列表 -->
      <el-col :span="18">
        <el-card class="box-card">
          <div slot="header" class="clearfix">
            <span>当前显示类别：<b style="color:#409EFF">{{ curCategoryName }}</b></span>
          </div>

          <!-- 操作栏 -->
          <div style="margin-bottom:12px;">
            <el-button type="success" @click="openAdd" :disabled="!curCategoryId">新增分组</el-button>
          </div>

          <!-- 表格 -->
          <el-table :data="tableData" style="width:100%" border>
            <el-table-column prop="id" label="编号" width="70"></el-table-column>
            <el-table-column prop="groupName" label="分组名称" width="140"></el-table-column>
            <el-table-column prop="descript" label="描述" min-width="160">
              <template slot-scope="scope">
                <span>{{ scope.row.descript || '-' }}</span>
              </template>
            </el-table-column>
            <el-table-column prop="icons" label="图标" width="120">
              <template slot-scope="scope">
                <span>{{ scope.row.icons || '-' }}</span>
              </template>
            </el-table-column>
            <el-table-column prop="sort" label="排序" width="80"></el-table-column>
            <el-table-column label="操作" width="140">
              <template slot-scope="scope">
                <el-button type="text" size="mini" @click="openEdit(scope.row)">编辑</el-button>
                <el-button type="text" size="mini" style="color:#f56c6c;" @click="doDelete(scope.row)">删除</el-button>
              </template>
            </el-table-column>
          </el-table>

          <!-- 分页 -->
          <el-pagination
            background
            layout="total, prev, pager, next"
            :total="totalCount"
            :current-page="curPage"
            :page-size="pageSize"
            @current-change="handlePageChange"
            style="margin-top:12px; text-align:right;">
          </el-pagination>
        </el-card>
      </el-col>
    </el-row>

    <!-- 新增/编辑对话框 -->
    <el-dialog :title="dialogTitle" :visible.sync="dialogVisible" width="500px" :close-on-click-modal="false">
      <el-form :model="groupForm" label-width="100px" ref="groupForm">
        <el-form-item label="分组名称" prop="groupName" required>
          <el-input v-model="groupForm.groupName" placeholder="请输入分组名称"></el-input>
        </el-form-item>
        <el-form-item label="所属分类">
          <el-input :value="curCategoryName" disabled></el-input>
        </el-form-item>
        <el-form-item label="描述">
          <el-input v-model="groupForm.descript" type="textarea" :rows="2" placeholder="请输入分组描述"></el-input>
        </el-form-item>
        <el-form-item label="图标">
          <el-input v-model="groupForm.icons" placeholder="请输入图标标识"></el-input>
        </el-form-item>
        <el-form-item label="排序">
          <el-input-number v-model="groupForm.sort" :min="0" :max="999"></el-input-number>
        </el-form-item>
      </el-form>
      <span slot="footer" class="dialog-footer">
        <el-button @click="dialogVisible=false">取消</el-button>
        <el-button type="primary" @click="doSave">确定</el-button>
      </span>
    </el-dialog>
  </div>
</template>

<script>
import {
  list,
  addAttrGroup,
  updateAttrGroup,
  deleteAttrGroup
} from '@/api/pms_attrGroup.js'
import { getCategoryList } from '@/api/pms_category.js'

export default {
  name: 'AttrGroupManage',
  data() {
    return {
      /* 类别树 */
      categoryList: [],
      defaultProps: { children: 'children', label: 'categoryName' },
      curCategoryId: null,
      curCategoryName: '',

      /* 表格 */
      tableData: [],
      totalCount: 0,
      curPage: 1,
      pageSize: 10,

      /* 对话框 */
      dialogVisible: false,
      dialogTitle: '',
      opType: '',
      groupForm: {}
    }
  },
  methods: {
    /* 加载类别树 */
    loadCategoryTree() {
      getCategoryList().then(resp => {
        this.categoryList = resp.data || []
      })
    },

    /* 点击类别节点 */
    handleNodeClick(data) {
      this.curCategoryId = data.id
      this.curCategoryName = data.categoryName
      this.curPage = 1
      this.loadList()
    },

    /* 加载列表 */
    loadList() {
      if (!this.curCategoryId) {
        this.tableData = []
        this.totalCount = 0
        return
      }
      list(this.curPage, this.pageSize, this.curCategoryId).then(resp => {
        this.tableData = resp.data || []
        this.totalCount = resp.total || 0
      })
    },

    /* 分页 */
    handlePageChange(page) {
      this.curPage = page
      this.loadList()
    },

    /* 打开新增 */
    openAdd() {
      this.opType = 'add'
      this.dialogTitle = '添加属性分组【所属类别：' + this.curCategoryName + '】'
      this.groupForm = {
        groupName: '',
        categoryId: this.curCategoryId,
        descript: '',
        icons: '',
        sort: 0
      }
      this.dialogVisible = true
    },

    /* 打开编辑 */
    openEdit(row) {
      this.opType = 'edit'
      this.dialogTitle = '编辑属性分组【' + row.groupName + '】'
      this.groupForm = {
        id: row.id,
        groupName: row.groupName,
        categoryId: this.curCategoryId,
        descript: row.descript || '',
        icons: row.icons || '',
        sort: row.sort || 0
      }
      this.dialogVisible = true
    },

    /* 保存 */
    doSave() {
      if (!this.groupForm.groupName) {
        this.$message.warning('请输入分组名称')
        return
      }
      if (this.opType == 'add') {
        addAttrGroup(this.groupForm).then(() => {
          this.$message.success('添加成功')
          this.dialogVisible = false
          this.loadList()
        })
      } else {
        updateAttrGroup(this.groupForm).then(() => {
          this.$message.success('更新成功')
          this.dialogVisible = false
          this.loadList()
        })
      }
    },

    /* 删除 */
    doDelete(row) {
      this.$confirm('确认删除分组【' + row.groupName + '】吗？', '提示', {
        type: 'warning'
      }).then(() => {
        deleteAttrGroup(row.id).then(() => {
          this.$message.success('删除成功')
          this.loadList()
        })
      }).catch(() => {})
    }
  },
  created() {
    this.loadCategoryTree()
  }
}
</script>

<style scoped>
.box-card { margin-bottom: 12px; }
</style>
```

### 4.3 router/index.js — 添加 /attrGroup 路由

**文件**：`D:\vue-space\mypro5\src\router\index.js`

**步骤 A**：在 import 区域添加：
```javascript
import AttrGroupManage from '@/components/AttrGroupManage'
```

**步骤 B**：在 routes 数组中添加（放在 `/category` 路由之后即可）：
```javascript
{
  path: '/attrGroup',
  name: 'attrGroup',
  component: AttrGroupManage
},
```

---

## 五、验收标准

### 后端
1. `mvn compile` 编译通过
2. 重启 MallAPP 后，POST `http://localhost:8090/mall-sys/AttrGroup/list/1/10/11` 返回7条数据（现有测试数据）
3. POST `/AttrGroup/add`，body `{"groupName":"测试分组","categoryId":11,"sort":99}` 返回 success
4. PUT `/AttrGroup/update`，body `{"id":新id,"groupName":"改名后"}` 返回 success
5. DELETE `/AttrGroup/delete/{id}` 返回 success

### 前端
1. `npm run build` 编译通过
2. 浏览器打开 `http://localhost:8080/#/attrGroup`，页面正常渲染（左侧类别树+右侧空表格）
3. 点击左侧分类（如手机），右侧显示该分类下的分组列表（7条测试数据）
4. 点击「新增分组」，填写名称后保存，列表刷新显示新分组
5. 点击「编辑」，修改名称后保存，列表更新
6. 点击「删除」，确认后记录从列表消失
7. 未选择分类时，「新增分组」按钮禁用

---

## 六、注意事项

1. **后端 Controller 是空壳**：交接文档原标注"后端已就绪"不准确，AttrGroupController 只有注释无端点，必须补全。这是本工单最大的坑。
2. **路由命名**：导航栏用的是 `#/attrGroup`（小驼峰），路由 path 必须写 `/attrGroup`，不能写成 `/attrgroup`。
3. **类别树复用**：直接用 `getCategoryList()` from `pms_category.js`，与 GoodsAttrManage.vue 完全一致，返回树形结构。
4. **分页接口签名**：list 接口用路径参数 `{page}/{limit}/{categoryId}`，不传 body，与 GoodsAttr 的 list（POST body）不同，注意区分。
5. **删除约束**：当前未实现"分组下有关联属性时禁止删除"的校验（课程作业级别可接受），若需加校验需查 tbl_group_attr_relation 中间表。
6. **webpack3 约束**：禁用可选链 `?.`，代码中已全部用 `||` 和 `&&` 代替。
7. **后端重启**：Java 代码修改后必须在 IDEA 重启 MallAPP。

---

## 七、修改文件清单

| # | 文件 | 操作 | 说明 |
|---|---|---|---|
| 1 | `IAttrGroupService.java` | 修改 | 加 deleteAttrGroup 方法签名 |
| 2 | `AttrGroupServiceImpl.java` | 修改 | 实现 deleteAttrGroup |
| 3 | `AttrGroupController.java` | 修改 | 补全 list/add/update/delete 4端点 |
| 4 | `pms_attrGroup.js` | 新建 | 4个API方法 |
| 5 | `AttrGroupManage.vue` | 新建 | 属性分组管理页面 |
| 6 | `router/index.js` | 修改 | 加 /attrGroup 路由 |
